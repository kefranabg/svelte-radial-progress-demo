import { type Readable } from 'svelte/store';
export declare function debounce<T>(value: Readable<T>, delayMs?: number): Readable<T>;
export declare function debounceTimer(delayMs?: number): Readable<{
    up: () => boolean;
}>;
